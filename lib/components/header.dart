import 'package:flutter/material.dart';

AppBar header(BuildContext context) {
  return AppBar(
    title: Text("ResourcsR"),
    elevation: 1,
    bottom: TabBar(
      tabs: [
        Tab(
          text: "",
        )
      ],
    ),
  );
}
